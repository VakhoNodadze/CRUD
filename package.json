{
  "scripts": {
    "build": "cd front && npm build",
    "install": "cd front && npm install",
    "heroku-postbuild": "npm run install && npm run build",
    "start": "node backend/node app.js"
  }
}