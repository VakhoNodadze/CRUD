{
  "scripts": {
    "build": "cd front && npm build",
    "install-frontend": "cd front && npm install",
    "install-backend": "cd backend && npm install",
    "heroku-postbuild": "npm run install-frontend && install-backend && npm run build",
    "start": "cd backend && node app.js"
  }
}